---
layout: main.liquid
tags: consultations
class: consultations
namespace: consultations
consultations-now: true
svg: placeholder
build-in: true
---

{% comment %} Menu and buttons translation {% endcomment %}
{% if page.lang == "id" %}
  {% assign menu = menu.id %}
  {% assign buttons = buttons.id %}
{% else %}
  {% assign menu = menu.en %}
  {% assign buttons = buttons.en %}
{% endif %}

<section class="hero">
  <article>
    <div class="text">
      <h4>{{ menu.consultations }}</h4>
      <h1>{{ title }}</h1>
      <p>{{ description-hero }}</p>
    </div>
    <a href="#request-demo" class="button cta">{{ buttons.request_quotation }}
      <i class="arrow"></i>
    </a>
  </article>
</section>

{{ content }}

{% if build-in %}
  {% include 'build-in' %}
{% endif %}

{% include 'form-request-for-demo' form-consultation: true %}