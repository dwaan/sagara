---
layout: main.liquid
tags: products
class: products
namespace: products
products-now: true
svg: placeholder
---

{% comment %} Menu and buttons translation {% endcomment %}
{% if page.lang == "id" %}
  {% assign menu = menu.id %}
  {% assign buttons = buttons.id %}
{% else %}
  {% assign menu = menu.en %}
  {% assign buttons = buttons.en %}
{% endif %}

{{ content }}

{% include 'build-in' %}

{% include 'form-request-for-demo' %}